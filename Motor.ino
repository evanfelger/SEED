#include <DualMC33926MotorShield.h>

DualMC33926MotorShield motor;


void setup() {
  // put your setup code here, to run once:
  unsigned long timeNow = 0;

}

void loop() {
  timeNow = millis();
  // put your main code here, to run repeatedly:
  analogWrite(0);
  Serial.print(

  
  while(millis() < time_now + 7){
        //wait approx. 7 ms
    }
  
}
